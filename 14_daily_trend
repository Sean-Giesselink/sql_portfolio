-- Question: Show the daily sales trend over time

SELECT
  DATE_TRUNC(DATE(created_at), DAY) AS day,
  ROUND(SUM(sale_price), 2) AS day_trend
FROM 
  `bigquery-public-data.thelook_ecommerce.order_items`
GROUP BY 
  day
ORDER BY 
  day
